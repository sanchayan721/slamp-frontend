import { styled } from "@mui/material";
import { Box } from "@mui/system";

const FlexCC = styled(Box)({
    height: '100%',
    width: '100%',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
});

export default FlexCC;