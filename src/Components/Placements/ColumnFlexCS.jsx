import { styled } from "@mui/material";
import FlexCC from "./FlexCC";

const ColumnFlexCS = styled(FlexCC)({
    flexDirection: 'column',
    justifyContent: 'start',
});

export default ColumnFlexCS;