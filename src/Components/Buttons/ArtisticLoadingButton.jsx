import { LoadingButton } from '@mui/lab'
import { styled } from '@mui/material'

const ArtisticLoadingButton = styled(LoadingButton)({
    borderRadius: '1.16em 0.3em',
    minHeight: '2em',
    minWidth: '5em',
    fontSize: '0.95em',
})

export default ArtisticLoadingButton